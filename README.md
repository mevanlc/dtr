# dtr

`dtr` means **do/develop the right repo**.

It is a repo-oriented command-line front end that accepts the repository
reference a person already has, resolves what that reference means, and invokes
the appropriate underlying tool.

The project is currently in planning. See [the MVP00 plan](devdocs/PLAN-MVP00.md).
